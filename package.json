{
  "name": "@spinworks-ai/surfaces-bridge",
  "version": "0.1.0",
  "description": "Reference bridge for connecting a local agent session to SpinWorks Surfaces.",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "surfaces-bridge": "./surfaces-bridge.mjs"
  },
  "files": [
    "README.md",
    "origin-policy.mjs",
    "release-source-manifest.json",
    "surfaces-bridge.mjs"
  ],
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "check": "node scripts/check-package.mjs && node scripts/check-release-workflow.mjs && npm pack --dry-run --json",
    "test": "node --test tests/keychain.test.mjs tests/origin-policy.test.mjs && npm run check && npm run test:clean-consumer",
    "test:clean-consumer": "node scripts/check-clean-consumer.mjs",
    "test:keychain-macos": "node --test tests/keychain-macos.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juanma-spinworks/surfaces-bridge.git"
  },
  "homepage": "https://surfaces.spinworks.ai",
  "bugs": {
    "url": "https://github.com/juanma-spinworks/surfaces-bridge/issues"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
